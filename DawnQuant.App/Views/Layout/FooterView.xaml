<UserControl x:Class="DawnQuant.App.Views.Layout.FooterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"  Loaded="MvxWpfView_Loaded" Unloaded="MvxWpfView_Unloaded"
             d:DesignHeight="25" d:DesignWidth="800">
    <Grid VerticalAlignment="Center" Margin="0,2,5,2" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBox  FontSize="12"  HorizontalAlignment="Left"
                 Width="180" >
            </TextBox>
            <TextBlock  x:Name="txtCurTime" Margin="5,0,10,0" VerticalAlignment="Center"></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
